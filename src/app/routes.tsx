import React from 'react';
import { Dashboard } from '@/pages/Dashboard';

export const AppRoutes: React.FC = () => {
    // Simple app routes mapping. Could use react-router here if multi-page scaling was needed immediately.
    return <Dashboard />;
};
